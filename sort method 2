# include <bits/stdc++.h>
using namespace std;
int x[4000];
int main(){
    int p;
    cin >> p;
    int q[p];
    for(int i = 0; i < p; i++){
        cin >> q[i];
    }
    for(int i  = 0; i < p; i++){
        x[q[i]] += 1;
    }
    for(int i  = 0; i< 4000; i++){
        for(int j  = 0; j < x[i]; j ++){
            cout << i << "  ";
        }
    }




    }
